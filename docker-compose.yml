services:
  database:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laracon
      MYSQL_USER: phpcon
      MYSQL_PASSWORD: root
    ports:
      - "3308:3306"
    volumes:
      - ./db:/var/lib/mysql
